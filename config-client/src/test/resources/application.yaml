server:
  port: 8081
  servlet:
    context-path: /app1
spring:
  cloud:
    config:
      enabled: false
  profiles:
    active: test
  application:
    name: app1
  config:
    import: optional:configserver:classpath:static/application.yaml

