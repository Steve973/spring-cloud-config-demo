pluginManagement {
    plugins {
        id 'org.springframework.boot' version "${springBootVersion}"
    }
}

rootProject.name = 'spring-cloud-config-demo'
include 'config-client'
include 'config-server'

